# Enable remote puzzle source downloads (OPTIONAL - disabled by default)
# Uncomment the line below to test actual puzzle downloads from registered sources
# Note: Some sources require authentication or may have CORS restrictions
# VITE_ENABLE_REMOTE_SOURCES=true

# Optional: Route download requests through a CORS proxy during dev
# Examples:
# VITE_CORS_PROXY_URL=https://corsproxy.io/?
# VITE_CORS_PROXY_URL=https://my-proxy.example.com/?target={url}
